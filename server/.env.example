# Copy this to server/.env and fill in real values before deploying.
# Never commit server/.env to version control.

# Required in production — generate a long random string
# Example: openssl rand -hex 32
JWT_SECRET=change_me_to_a_long_random_secret

# Port the server listens on (defaults to 3001)
PORT=3001

# Comma-separated list of allowed CORS origins
# Defaults to localhost dev origins if not set
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
